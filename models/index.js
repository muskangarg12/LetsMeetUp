// Import all DB Models
const User = require("./user");
const Chat = require("./chat");
const Room = require("./room");
const ip = require("./ip");

// Export all the models
module.exports = { User, Chat, Room, ip};
